<UserControl x:Class="WorkTogether.WPF.PageList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:av="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av" 
             Background="White" av:DesignHeight="502.036" av:DesignWidth="827.069">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleLabel" Grid.Row="0" Text="Titre" FontSize="22" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <Frame x:Name="ListFrame"
                Grid.Column="0"
                NavigationUIVisibility="Hidden"/>

            <Frame x:Name="FormFrame"
                Grid.Column="1"
                NavigationUIVisibility="Hidden"/>

        </Grid>
    </Grid>
</UserControl>
